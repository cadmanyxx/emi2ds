    //Funções

    let x ='nada'
    console.log(x)

    x = 'oi'
    imprimetexto = (x)
    console.log(x)

    // Declarar uma função
    // estrutura nomeDafunção (parâmetros) { funcionalidade}
    function imprimetexto(){
        console.log(texto)
    }

    imprimeTexto('Sou o mais capudo do free fire')
    imprimeTexto('Sou muito bom')

    function soma() {
        return 2+2
    }
    imprimeTexto(soma())
    //Console.log(soma())
    function somaDenumero(numero1, numero2){
        return numero1 + numero2
    }

    imprimeTexto(somaDeNumero(10, 8))
    function divisaoDeNumeros(numero1, numero2){
        return numero1 / numero2
    }

    imrpimeTexto (divisaoDeNumeros(10, 2))
    imprimeTexto(divisaoDeNumeros(2, 10))

